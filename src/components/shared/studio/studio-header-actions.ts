/**
 * Studio page header actions — Share / Create analytics on `/analytics`,
 * Undo changes / Save on sermon edit (same size, border, and teal primary).
 */
export const STUDIO_HEADER_ACTION = {
    outline:
        'h-8 gap-2 rounded-sm border-[#545454]/50 text-[#bdbdbd]',
    primary:
        'h-8 gap-2 rounded-sm bg-[#08ffdb] text-[#1f2020] hover:bg-[#08ffdb]/90',
} as const;
